div.auth-attention.banner
  span(l10n) Your Ripple account is not active. To activate it, you'll need to send at least 20 XRP to ({{address}}).
  br
  a(href="#/activate/{{address}}", l10n) Click to activate for free
